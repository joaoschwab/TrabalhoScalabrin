package com.company;

public class Recursivos {

    public class quickSort(int vetor[], int inicio, int fim){
        if(vetor[].)

    }
}
    def quickSort(vetor, inicio, fim):
        if len(vetor) == 1:
        return vetor

        if inicio < fim:
        indiceDaParticao = partition(vetor, inicio, fim)

        quickSort(vetor, inicio, indiceDaParticao - 1)
        quickSort(vetor, indiceDaParticao + 1, fim)